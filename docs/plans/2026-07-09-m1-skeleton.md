# M1：骨架打通 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 端到端跑通最小闭环——`docker-compose up` 起全栈，用 TS SDK 上报一条 trace，Web UI 列表展示出来。

**Architecture:** pnpm monorepo，三个 package（server / web / sdk-ts）+ shared 类型包。Fastify 后端通过 Drizzle ORM 操作 Postgres；SDK 用 AsyncLocalStorage 维护父子上下文批量上报；Next.js 前端渲染 trace 列表。Repository 接口抽象数据库访问。

**Tech Stack:** TypeScript、pnpm workspaces、Fastify、Drizzle ORM、PostgreSQL、Vitest、Zod、Next.js(App Router)、React、Tailwind、Docker Compose

**配套设计规格:** [docs/specs/2026-07-09-openagenttelemetry-platform-design.md](../specs/2026-07-09-openagenttelemetry-platform-design.md)

---

## 文件结构总览（M1 完成后）

```
OpenAgentTelemetry/
├── package.json                  # 根 package（工作区配置）
├── pnpm-workspace.yaml           # 工作区声明
├── tsconfig.base.json            # 共享 TS 配置
├── .gitignore
├── docker-compose.yml            # Postgres + Redis + server + web
├── packages/
│   └── shared/
│       ├── package.json
│       ├── tsconfig.json
│       ├── src/
│       │   ├── index.ts          # 导出所有类型
│       │   └── ingestion.ts      # ingestion 请求/响应类型 + Zod schema
│       └── tests/ingestion.test.ts
├── apps/
│   ├── server/
│   │   ├── package.json
│   │   ├── tsconfig.json
│   │   ├── drizzle.config.ts
│   │   ├── src/
│   │   │   ├── db/
│   │   │   │   ├── client.ts        # Drizzle 客户端
│   │   │   │   └── schema.ts        # 表定义
│   │   │   ├── repositories/
│   │   │   │   ├── trace-repository.ts   # 接口 + Postgres 实现
│   │   │   │   └── project-repository.ts
│   │   │   ├── routes/
│   │   │   │   ├── health.ts
│   │   │   │   └── ingestion.ts
│   │   │   ├── modules/
│   │   │   │   └── ingestion-service.ts
│   │   │   ├── app.ts              # Fastify app 工厂
│   │   │   └── server.ts           # 启动入口
│   │   └── tests/
│   │       ├── ingestion-api.test.ts
│   │       └── trace-repository.test.ts
│   ├── web/
│   │   ├── package.json
│   │   ├── next.config.js
│   │   ├── tsconfig.json
│   │   ├── tailwind.config.ts
│   │   ├── src/app/
│   │   │   ├── layout.tsx
│   │   │   ├── page.tsx            # trace 列表页
│   │   │   └── globals.css
│   │   └── src/lib/api.ts
│   └── sdk-ts/
│       ├── package.json
│       ├── tsconfig.json
│       ├── src/
│       │   ├── index.ts
│       │   ├── client.ts           # HTTP 客户端 + 批量
│       │   ├── context.ts          # AsyncLocalStorage 上下文
│       │   └── traceable.ts        # 装饰器
│       └── tests/
│           ├── context.test.ts
│           └── client.test.ts
```

**职责说明：**
- `packages/shared`：API 契约类型，前后端 + SDK 共用，改一处全联动。
- `apps/server/src/db/schema.ts`：Drizzle 表定义，是 SQL 结构的唯一真相。
- `apps/server/src/repositories`：Repository 接口 + Postgres 实现，业务层不直接碰 SQL。
- `apps/sdk-ts/src/context.ts`：AsyncLocalStorage 维护父子关系，是自动成树的核心。
- `apps/web`：Next.js 前端。

---

## Task 1: Monorepo 脚手架与工具链

**Files:**
- Create: `package.json`
- Create: `pnpm-workspace.yaml`
- Create: `tsconfig.base.json`
- Create: `.gitignore`

- [ ] **Step 1: 创建 pnpm 工作区配置**

Create `pnpm-workspace.yaml`:
```yaml
packages:
  - "apps/*"
  - "packages/*"
```
> 为什么：告诉 pnpm 这些目录是工作区包，包之间可以互相引用，且共享 `node_modules`（省磁盘、装得快）。

- [ ] **Step 2: 创建根 package.json**

Create `package.json`:
```json
{
  "name": "openagenttelemetry",
  "private": true,
  "version": "0.0.0",
  "description": "开源 AI Agent 可观测性平台",
  "scripts": {
    "dev:server": "pnpm --filter @oat/server dev",
    "dev:web": "pnpm --filter @oat/web dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "packageManager": "pnpm@9.0.0"
}
```

- [ ] **Step 3: 创建共享 TS 基础配置**

Create `tsconfig.base.json`:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true
  }
}
```
> 为什么 strict: true：TS 新手最大的安全感来自编译期检查，strict 会拦住大量隐性错误。

- [ ] **Step 4: 创建 .gitignore**

Create `.gitignore`:
```
node_modules/
dist/
.next/
.env
.env.local
*.log
.DS_Store
coverage/
```

- [ ] **Step 5: 安装依赖并验证工作区**

Run:
```bash
pnpm install
```
Expected: 安装成功，生成 `pnpm-lock.yaml`。

- [ ] **Step 6: 提交**

```bash
git add -A
git commit -m "chore: 初始化 pnpm monorepo 脚手架"
```

---

## Task 2: shared 包——API 契约类型（TDD）

**Files:**
- Create: `packages/shared/package.json`
- Create: `packages/shared/tsconfig.json`
- Create: `packages/shared/src/ingestion.ts`
- Create: `packages/shared/src/index.ts`
- Test: `packages/shared/tests/ingestion.test.ts`

> 为什么先做 shared：它是前后端和 SDK 的"合同"。先定合同，再分别实现各方，避免接口漂移。

- [ ] **Step 1: 初始化 shared 包**

Create `packages/shared/package.json`:
```json
{
  "name": "@oat/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
```

Create `packages/shared/tsconfig.json`:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*", "tests/**/*"]
}
```

- [ ] **Step 2: 写失败测试——校验 ingestion schema**

Create `packages/shared/tests/ingestion.test.ts`:
```typescript
import { describe, it, expect } from 'vitest';
import { ingestionBatchSchema } from '../src/ingestion';

describe('ingestionBatchSchema', () => {
  it('接受合法的批量上报', () => {
    const valid = {
      batch: [
        {
          id: 'obs-1',
          traceId: 'trace-1',
          parentId: null,
          type: 'span',
          name: 'root',
          startTime: '2026-07-09T00:00:00Z',
          endTime: '2026-07-09T00:00:01Z',
          input: { q: 'hello' },
          output: { a: 'hi' },
          metadata: {},
        },
      ],
    };
    expect(() => ingestionBatchSchema.parse(valid)).not.toThrow();
  });

  it('拒绝缺少 traceId 的观测点', () => {
    const invalid = { batch: [{ id: 'obs-1' }] };
    expect(() => ingestionBatchSchema.parse(invalid)).toThrow();
  });

  it('拒绝无效的 type', () => {
    const invalid = {
      batch: [
        {
          id: '1', traceId: 't1', parentId: null,
          type: 'invalid-type', name: 'x',
          startTime: '2026-07-09T00:00:00Z',
        },
      ],
    };
    expect(() => ingestionBatchSchema.parse(invalid)).toThrow();
  });
});
```

- [ ] **Step 3: 运行测试确认失败**

Run: `pnpm --filter @oat/shared test`
Expected: FAIL，`ingestionBatchSchema` 未定义。

- [ ] **Step 4: 实现 ingestion schema 与类型**

Create `packages/shared/src/ingestion.ts`:
```typescript
import { z } from 'zod';

// 观测点类型：span=有时长步骤；event=瞬时事件；generation=LLM 调用
export const observationTypeSchema = z.enum(['span', 'event', 'generation']);

// 单个观测点的运行时校验（SDK 上报的数据形态多变，必须校验）
export const observationSchema = z.object({
  id: z.string(),
  traceId: z.string(),
  parentId: z.string().nullable().default(null),
  type: observationTypeSchema,
  name: z.string(),
  startTime: z.string(),
  endTime: z.string().optional(),
  input: z.any().optional(),
  output: z.any().optional(),
  model: z.string().optional(),
  promptTokens: z.number().int().nonnegative().optional(),
  completionTokens: z.number().int().nonnegative().optional(),
  totalCost: z.number().nonnegative().optional(),
  level: z.enum(['debug', 'info', 'warning', 'error']).optional(),
  metadata: z.record(z.unknown()).optional(),
  userId: z.string().optional(),
  sessionId: z.string().optional(),
});

// 批量上报请求
export const ingestionBatchSchema = z.object({
  batch: z.array(observationSchema).min(1),
});

// 从 schema 反向推导出 TS 类型（schema 与类型永不漂移）
export type Observation = z.infer<typeof observationSchema>;
export type IngestionBatchRequest = z.infer<typeof ingestionBatchSchema>;
```
> 为什么 `z.infer`：Zod schema 既是运行时校验器，又是 TS 类型的来源，一份代码两用，杜绝"类型与实际数据不一致"。

Create `packages/shared/src/index.ts`:
```typescript
export * from './ingestion.js';
```
> 为什么加 `.js` 后缀：ESM 模式下 TS 要求 import 路径带扩展名（即使是 .ts 源文件也写 .js），这是 Node ESM 规范。

- [ ] **Step 5: 运行测试确认通过**

Run: `pnpm --filter @oat/shared test`
Expected: 3 个测试 PASS。

- [ ] **Step 6: 类型检查**

Run: `pnpm --filter @oat/shared lint`
Expected: 无错误。

- [ ] **Step 7: 提交**

```bash
git add -A
git commit -m "feat(shared): 添加 ingestion API 契约类型与 Zod 校验"
```

---

## Task 3: 数据库 Schema（Drizzle）

**Files:**
- Create: `apps/server/package.json`
- Create: `apps/server/tsconfig.json`
- Create: `apps/server/drizzle.config.ts`
- Create: `apps/server/src/db/schema.ts`

- [ ] **Step 1: 初始化 server 包**

Create `apps/server/package.json`:
```json
{
  "name": "@oat/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@oat/shared": "workspace:*",
    "fastify": "^4.27.0",
    "@fastify/cors": "^9.0.0",
    "drizzle-orm": "^0.31.0",
    "postgres": "^3.4.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "drizzle-kit": "^0.22.0",
    "tsx": "^4.15.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
```
> 为什么 `workspace:*`：从本地工作区引用 shared 包，pnpm 会建立软链接，改 shared 立即生效。

Create `apps/server/tsconfig.json`:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*", "tests/**/*"]
}
```

- [ ] **Step 2: 安装 server 依赖**

Run: `pnpm install`
Expected: 安装 fastify、drizzle-orm 等成功。

- [ ] **Step 3: 编写 Drizzle 表定义**

Create `apps/server/src/db/schema.ts`:
```typescript
import { pgTable, uuid, text, timestamp, jsonb, integer, numeric, pgEnum } from 'drizzle-orm/pg-core';

// 枚举：观测点类型
export const observationTypeEnum = pgEnum('observation_type', ['span', 'event', 'generation']);
export const observationLevelEnum = pgEnum('observation_level', ['debug', 'info', 'warning', 'error']);

// projects：数据隔离边界
export const projects = pgTable('projects', {
  id: uuid('id').primaryKey().defaultRandom(),
  name: text('name').notNull(),
  slug: text('slug').notNull().unique(),
  apiKey: text('api_key').notNull().unique(),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).defaultNow().notNull(),
});

// traces：调用链的根
export const traces = pgTable('traces', {
  id: uuid('id').primaryKey().defaultRandom(),
  projectId: uuid('project_id').notNull().references(() => projects.id),
  name: text('name').notNull(),
  userId: text('user_id'),
  sessionId: text('session_id'),
  input: jsonb('input'),
  output: jsonb('output'),
  metadata: jsonb('metadata'),
  timestamp: timestamp('timestamp', { withTimezone: true }).defaultNow().notNull(),
});

// observations：树上的节点（核心表）
export const observations = pgTable('observations', {
  id: uuid('id').primaryKey().defaultRandom(),
  traceId: uuid('trace_id').notNull().references(() => traces.id),
  parentId: uuid('parent_id'),
  type: observationTypeEnum('type').notNull(),
  name: text('name').notNull(),
  startTime: timestamp('start_time', { withTimezone: true }).notNull(),
  endTime: timestamp('end_time', { withTimezone: true }),
  input: jsonb('input'),
  output: jsonb('output'),
  model: text('model'),
  promptTokens: integer('prompt_tokens'),
  completionTokens: integer('completion_tokens'),
  totalCost: numeric('total_cost', { precision: 12, scale: 6 }),
  level: observationLevelEnum('level'),
  metadata: jsonb('metadata'),
});
```
> 为什么 traces 和 observations 分两张表：一个 trace 是聚合根（按用户/会话/时间筛选），observations 是明细。分开后列表页只查 traces（快），展开树时再查 observations。

- [ ] **Step 4: 编写 Drizzle 配置**

Create `apps/server/drizzle.config.ts`:
```typescript
import { defineConfig } from 'drizzle-kit';

export default defineConfig({
  schema: './src/db/schema.ts',
  out: './drizzle',
  dialect: 'postgresql',
  dbCredentials: {
    url: process.env.DATABASE_URL ?? 'postgresql://oat:oat@localhost:5432/oat',
  },
});
```

- [ ] **Step 5: 生成第一个迁移文件**

确保本地有 Postgres（可用 docker 临时起，见 Task 8）。先在仓库根准备 `.env`（不提交）：
```bash
echo 'DATABASE_URL=postgresql://oat:oat@localhost:5432/oat' > apps/server/.env
```

Run: `pnpm --filter @oat/server db:generate`
Expected: 在 `apps/server/drizzle/` 下生成 `0000_xxx.sql` 迁移文件，含建表语句。

- [ ] **Step 6: 类型检查**

Run: `pnpm --filter @oat/server lint`
Expected: 无错误。

- [ ] **Step 7: 提交**

```bash
git add -A
git commit -m "feat(server): 添加 Drizzle 数据库 schema（projects/traces/observations）"
```

---

## Task 4: 数据库客户端与 Repository 层（TDD）

**Files:**
- Create: `apps/server/src/db/client.ts`
- Create: `apps/server/src/repositories/trace-repository.ts`
- Create: `apps/server/src/repositories/project-repository.ts`
- Test: `apps/server/tests/trace-repository.test.ts`

> 为什么 Repository 层：所有 SQL 收敛到接口后面，业务逻辑只调方法。未来换 ClickHouse 只需新写实现，这是方案 A+ 的精髓。

- [ ] **Step 1: 编写数据库客户端**

Create `apps/server/src/db/client.ts`:
```typescript
import { drizzle } from 'drizzle-orm/postgres-js';
import postgres from 'postgres';
import * as schema from './schema.js';

// 从环境变量读连接串，给默认值方便本地开发
const connectionString = process.env.DATABASE_URL ?? 'postgresql://oat:oat@localhost:5432/oat';

// postgres 客户端（用于实际查询）
const queryClient = postgres(connectionString);

// drizzle 实例，绑定 schema 以便关系查询
export const db = drizzle(queryClient, { schema });
export { schema };
```

- [ ] **Step 2: 编写 Repository 接口与实现**

Create `apps/server/src/repositories/trace-repository.ts`:
```typescript
import { db, schema } from '../db/client.js';
import { eq, desc } from 'drizzle-orm';
import type { Observation } from '@oat/shared';

// 接口定义：业务层依赖接口而非实现（为未来换存储预留）
export interface ITraceRepository {
  listTraces(projectId: string, limit: number): Promise<TraceListItem[]>;
  createTraceWithObservations(trace: NewTrace, observations: Observation[]): Promise<void>;
}

export type TraceListItem = {
  id: string;
  name: string;
  userId: string | null;
  sessionId: string | null;
  timestamp: Date;
};

export type NewTrace = {
  id?: string;
  projectId: string;
  name: string;
  userId?: string | null;
  sessionId?: string | null;
  input?: unknown;
  output?: unknown;
  metadata?: Record<string, unknown>;
};

// Postgres 实现
export class PostgresTraceRepository implements ITraceRepository {
  async listTraces(projectId: string, limit = 50): Promise<TraceListItem[]> {
    const rows = await db
      .select({
        id: schema.traces.id,
        name: schema.traces.name,
        userId: schema.traces.userId,
        sessionId: schema.traces.sessionId,
        timestamp: schema.traces.timestamp,
      })
      .from(schema.traces)
      .where(eq(schema.traces.projectId, projectId))
      .orderBy(desc(schema.traces.timestamp))
      .limit(limit);
    return rows;
  }

  async createTraceWithObservations(trace: NewTrace, observations: Observation[]): Promise<void> {
    // 用事务保证一致性：trace 和它的 observations 要么都成功要么都回滚
    await db.transaction(async (tx) => {
      const [inserted] = await tx
        .insert(schema.traces)
        .values({
          projectId: trace.projectId,
          name: trace.name,
          userId: trace.userId ?? null,
          sessionId: trace.sessionId ?? null,
          input: trace.input,
          output: trace.output,
          metadata: trace.metadata,
        })
        .returning({ id: schema.traces.id });

      if (observations.length > 0) {
        await tx.insert(schema.observations).values(
          observations.map((o) => ({
            traceId: inserted.id,
            parentId: o.parentId ?? null,
            type: o.type,
            name: o.name,
            startTime: new Date(o.startTime),
            endTime: o.endTime ? new Date(o.endTime) : null,
            input: o.input,
            output: o.output,
            model: o.model,
            promptTokens: o.promptTokens,
            completionTokens: o.completionTokens,
            totalCost: o.totalCost?.toString(),
            level: o.level,
            metadata: o.metadata,
          })),
        );
      }
    });
  }
}
```
> 为什么用事务：一个 trace 和它的 observations 是逻辑整体，事务防止"trace 写成功但 observations 失败"的脏数据。

- [ ] **Step 3: 编写 project repository（按 apiKey 查 project）**

Create `apps/server/src/repositories/project-repository.ts`:
```typescript
import { db, schema } from '../db/client.js';
import { eq } from 'drizzle-orm';

export interface IProjectRepository {
  findByApiKey(apiKey: string): Promise<{ id: string; name: string } | null>;
}

export class PostgresProjectRepository implements IProjectRepository {
  async findByApiKey(apiKey: string) {
    const [row] = await db
      .select({ id: schema.projects.id, name: schema.projects.name })
      .from(schema.projects)
      .where(eq(schema.projects.apiKey, apiKey));
    return row ?? null;
  }
}
```

- [ ] **Step 4: 写集成测试（用真实 Postgres）**

> 测试需要真实 Postgres。先在 Task 8 配好 docker-compose 后再补跑，这里先写好测试代码。

Create `apps/server/tests/trace-repository.test.ts`:
```typescript
import { describe, it, expect, beforeAll } from 'vitest';
import { PostgresTraceRepository, PostgresProjectRepository } from '../src/repositories/index.js';

// 这些测试需要 DATABASE_URL 指向一个可写的 Postgres
// 本地跑前先启动 docker-compose（见 Task 8）
const repo = new PostgresTraceRepository();
const projectRepo = new PostgresProjectRepository();

describe('PostgresTraceRepository（需真实 Postgres）', () => {
  it('能写入 trace 并列出', async () => {
    const project = await projectRepo.findByApiKey('test-key');
    if (!project) return; // 种子数据未就绪时跳过

    await repo.createTraceWithObservations(
      { projectId: project.id, name: '测试 trace' },
      [],
    );

    const list = await repo.listTraces(project.id, 10);
    expect(list.some((t) => t.name === '测试 trace')).toBe(true);
  });
});
```

- [ ] **Step 5: 创建 repositories barrel 导出**

Create `apps/server/src/repositories/index.ts`:
```typescript
export * from './trace-repository.js';
export * from './project-repository.js';
```

- [ ] **Step 6: 类型检查**

Run: `pnpm --filter @oat/server lint`
Expected: 无错误。

- [ ] **Step 7: 提交**

```bash
git add -A
git commit -m "feat(server): 添加数据库客户端与 Repository 层（接口+Postgres 实现）"
```

---

## Task 5: Fastify 服务与 Ingestion API（TDD）

**Files:**
- Create: `apps/server/src/app.ts`
- Create: `apps/server/src/server.ts`
- Create: `apps/server/src/routes/health.ts`
- Create: `apps/server/src/routes/ingestion.ts`
- Create: `apps/server/src/modules/ingestion-service.ts`
- Test: `apps/server/tests/ingestion-api.test.ts`

- [ ] **Step 1: 编写 app 工厂（可注入依赖，便于测试）**

Create `apps/server/src/app.ts`:
```typescript
import Fastify, { type FastifyInstance } from 'fastify';
import cors from '@fastify/cors';
import { healthRoutes } from './routes/health.js';
import { ingestionRoutes } from './routes/ingestion.js';

// app 工厂：把依赖作为参数传入，测试时可传 mock
export interface AppDeps {
  traceRepo: import('./repositories/trace-repository.js').ITraceRepository;
  projectRepo: import('./repositories/project-repository.js').IProjectRepository;
}

export async function buildApp(deps: AppDeps): Promise<FastifyInstance> {
  const app = Fastify({ logger: true });
  await app.register(cors, { origin: true }); // 允许前端跨域

  await app.register(healthRoutes);
  await app.register(ingestionRoutes, { deps });

  return app;
}
```
> 为什么依赖注入：测试时可以传内存 mock 的 repo，不需要真实数据库就能测 API 逻辑。这是把"API 逻辑"与"存储实现"解耦的关键。

- [ ] **Step 2: 健康检查路由**

Create `apps/server/src/routes/health.ts`:
```typescript
import type { FastifyPluginAsync } from 'fastify';

export const healthRoutes: FastifyPluginAsync = async (app) => {
  app.get('/health', async () => ({ status: 'ok' }));
};
```

- [ ] **Step 3: ingestion service（业务逻辑）**

Create `apps/server/src/modules/ingestion-service.ts`:
```typescript
import type { ITraceRepository } from '../repositories/trace-repository.js';
import type { Observation } from '@oat/shared';

export class IngestionService {
  constructor(private traceRepo: ITraceRepository) {}

  // 把一批 observations 按 traceId 分组，每组建一个 trace
  async ingest(projectId: string, observations: Observation[]) {
    const grouped = new Map<string, Observation[]>();
    for (const o of observations) {
      const list = grouped.get(o.traceId) ?? [];
      list.push(o);
      grouped.set(o.traceId, list);
    }

    for (const [traceId, obs] of grouped) {
      // trace 名取第一个 observation 的 name，或用 traceId
      const name = obs.find((o) => o.parentId == null)?.name ?? `trace-${traceId}`;
      await this.traceRepo.createTraceWithObservations(
        {
          projectId,
          name,
          userId: obs[0]?.userId ?? null,
          sessionId: obs[0]?.sessionId ?? null,
          input: obs[0]?.input,
          output: obs.find((o) => o.parentId == null)?.output,
          metadata: obs[0]?.metadata,
        },
        obs,
      );
    }
  }
}
```

- [ ] **Step 4: ingestion 路由（校验 + 鉴权 + 调 service）**

Create `apps/server/src/routes/ingestion.ts`:
```typescript
import type { FastifyPluginAsync } from 'fastify';
import { ingestionBatchSchema } from '@oat/shared';
import type { AppDeps } from '../app.js';
import { IngestionService } from '../modules/ingestion-service.js';

export const ingestionRoutes: FastifyPluginAsync<AppDeps> = async (app, opts) => {
  const service = new IngestionService(opts.deps.traceRepo);

  app.post('/api/public/ingestion', async (req, reply) => {
    // 1. 用 API Key 鉴权
    const authHeader = req.headers.authorization;
    const apiKey = authHeader?.replace('Bearer ', '');
    if (!apiKey) return reply.code(401).send({ error: { code: 'UNAUTHORIZED', message: '缺少 API Key' } });

    const project = await opts.deps.projectRepo.findByApiKey(apiKey);
    if (!project) return reply.code(401).send({ error: { code: 'UNAUTHORIZED', message: '无效的 API Key' } });

    // 2. 用 Zod 校验请求体
    const parsed = ingestionBatchSchema.safeParse(req.body);
    if (!parsed.success) {
      return reply.code(400).send({ error: { code: 'BAD_REQUEST', message: parsed.error.message } });
    }

    // 3. 执行写入
    await service.ingest(project.id, parsed.data.batch);

    // 4. 立即返回 202（M1 先同步写；后续 M2 改为入队异步）
    return reply.code(202).send({ accepted: parsed.data.batch.length });
  });
};
```

- [ ] **Step 5: 写 API 测试（用内存 mock repo，不依赖数据库）**

Create `apps/server/tests/ingestion-api.test.ts`:
```typescript
import { describe, it, expect, beforeEach } from 'vitest';
import type { FastifyInstance } from 'fastify';
import { buildApp } from '../src/app.js';
import type { ITraceRepository, IProjectRepository } from '../src/repositories/index.js';

// 内存 mock：记录被调用的数据，方便断言
function makeMockRepos() {
  const stored: any[] = [];
  const traceRepo: ITraceRepository = {
    async listTraces() { return []; },
    async createTraceWithObservations(trace, obs) { stored.push({ trace, obs }); },
  };
  const projectRepo: IProjectRepository = {
    async findByApiKey(key: string) {
      return key === 'valid-key' ? { id: 'proj-1', name: 'test' } : null;
    },
  };
  return { traceRepo, projectRepo, stored };
}

describe('Ingestion API', () => {
  let app: FastifyInstance;
  let mocks: ReturnType<typeof makeMockRepos>;

  beforeEach(async () => {
    mocks = makeMockRepos();
    app = await buildApp(mocks);
  });

  it('缺少 API Key 返回 401', async () => {
    const res = await app.inject({ method: 'POST', url: '/api/public/ingestion', payload: { batch: [] } });
    expect(res.statusCode).toBe(401);
  });

  it('无效 API Key 返回 401', async () => {
    const res = await app.inject({
      method: 'POST', url: '/api/public/ingestion',
      headers: { authorization: 'Bearer wrong' },
      payload: { batch: [] },
    });
    expect(res.statusCode).toBe(401);
  });

  it('合法请求写入并返回 202', async () => {
    const res = await app.inject({
      method: 'POST', url: '/api/public/ingestion',
      headers: { authorization: 'Bearer valid-key' },
      payload: {
        batch: [
          {
            id: 'o1', traceId: 't1', parentId: null,
            type: 'span', name: 'root', startTime: '2026-07-09T00:00:00Z',
          },
        ],
      },
    });
    expect(res.statusCode).toBe(202);
    expect(JSON.parse(res.payload).accepted).toBe(1);
    expect(mocks.stored).toHaveLength(1);
  });

  it('坏数据返回 400', async () => {
    const res = await app.inject({
      method: 'POST', url: '/api/public/ingestion',
      headers: { authorization: 'Bearer valid-key' },
      payload: { batch: [{ id: 'x' }] }, // 缺 traceId
    });
    expect(res.statusCode).toBe(400);
  });
});
```
> 为什么用 `app.inject`：Fastify 的 inject 方法不发真实网络请求，直接在内存里调用路由，测试快且无需占端口。

- [ ] **Step 6: 运行测试**

Run: `pnpm --filter @oat/server test`
Expected: 4 个 API 测试 PASS。

- [ ] **Step 7: 编写启动入口**

Create `apps/server/src/server.ts`:
```typescript
import { buildApp } from './app.js';
import { PostgresTraceRepository, PostgresProjectRepository } from './repositories/index.js';

async function main() {
  const app = await buildApp({
    traceRepo: new PostgresTraceRepository(),
    projectRepo: new PostgresProjectRepository(),
  });

  const port = Number(process.env.PORT ?? 3001);
  await app.listen({ port, host: '0.0.0.0' });
  app.log.info(`Server 监听 http://localhost:${port}`);
}

main().catch((err) => {
  console.error(err);
  process.exit(1);
});
```

- [ ] **Step 8: 类型检查**

Run: `pnpm --filter @oat/server lint`
Expected: 无错误。

- [ ] **Step 9: 提交**

```bash
git add -A
git commit -m "feat(server): 添加 Fastify 服务与 Ingestion API（鉴权+校验+写入）"
```

---

## Task 6: 新增 traces 查询 API

> Web 前端列表页需要一个查询接口。复用已建的 Repository。

**Files:**
- Create: `apps/server/src/routes/traces.ts`
- Modify: `apps/server/src/app.ts`
- Modify: `apps/server/tests/ingestion-api.test.ts`（加查询测试）

- [ ] **Step 1: 写失败测试——列出 traces**

在 `apps/server/tests/ingestion-api.test.ts` 末尾追加：
```typescript
import type { TraceListItem } from '../src/repositories/trace-repository.js';

describe('Traces 查询 API', () => {
  it('返回项目下的 trace 列表', async () => {
    const listTraceReturn: TraceListItem[] = [
      { id: 't1', name: 'trace1', userId: null, sessionId: null, timestamp: new Date() },
    ];
    const traceRepo: ITraceRepository = {
      async listTraces() { return listTraceReturn; },
      async createTraceWithObservations() {},
    };
    const projectRepo: IProjectRepository = {
      async findByApiKey(k: string) { return k === 'valid-key' ? { id: 'proj-1', name: 't' } : null; },
    };
    const app = await buildApp({ traceRepo, projectRepo });

    const res = await app.inject({ method: 'GET', url: '/api/traces?projectId=proj-1' });
    expect(res.statusCode).toBe(200);
    const body = JSON.parse(res.payload);
    expect(body.traces).toHaveLength(1);
    expect(body.traces[0].name).toBe('trace1');
  });
});
```

更新 mock 工厂，让 `listTraces` 可配置：
```typescript
function makeMockRepos(listReturn: TraceListItem[] = []) {
  const stored: any[] = [];
  const traceRepo: ITraceRepository = {
    async listTraces() { return listReturn; },
    async createTraceWithObservations(trace, obs) { stored.push({ trace, obs }); },
  };
  // ... projectRepo 不变
}
```
（第一个 describe 用 `makeMockRepos([])`）

- [ ] **Step 2: 运行测试确认失败**

Run: `pnpm --filter @oat/server test`
Expected: 新测试 FAIL，`/api/traces` 路由不存在（404）。

- [ ] **Step 3: 实现 traces 查询路由**

Create `apps/server/src/routes/traces.ts`:
```typescript
import type { FastifyPluginAsync } from 'fastify';
import { z } from 'zod';
import type { AppDeps } from '../app.js';

const querySchema = z.object({
  projectId: z.string().uuid(),
  limit: z.coerce.number().int().min(1).max(200).default(50),
});

export const tracesRoutes: FastifyPluginAsync<AppDeps> = async (app, opts) => {
  app.get('/api/traces', async (req, reply) => {
    const parsed = querySchema.safeParse(req.query);
    if (!parsed.success) {
      return reply.code(400).send({ error: { code: 'BAD_REQUEST', message: parsed.error.message } });
    }
    const { projectId, limit } = parsed.data;
    const traces = await opts.deps.traceRepo.listTraces(projectId, limit);
    return { traces };
  });
};
```

- [ ] **Step 4: 在 app.ts 注册 traces 路由**

Modify `apps/server/src/app.ts`，在 ingestion 注册后加：
```typescript
import { tracesRoutes } from './routes/traces.js';
// ... 在 ingestionRoutes 之后：
await app.register(tracesRoutes, { deps });
```

- [ ] **Step 5: 运行测试确认通过**

Run: `pnpm --filter @oat/server test`
Expected: 全部 PASS（含新查询测试）。

- [ ] **Step 6: 类型检查**

Run: `pnpm --filter @oat/server lint`
Expected: 无错误。

- [ ] **Step 7: 提交**

```bash
git add -A
git commit -m "feat(server): 添加 traces 列表查询 API"
```

---

## Task 7: SDK-TS——上下文与批量客户端（TDD）

**Files:**
- Create: `apps/sdk-ts/package.json`
- Create: `apps/sdk-ts/tsconfig.json`
- Create: `apps/sdk-ts/src/context.ts`
- Create: `apps/sdk-ts/src/client.ts`
- Create: `apps/sdk-ts/src/traceable.ts`
- Create: `apps/sdk-ts/src/index.ts`
- Test: `apps/sdk-ts/tests/context.test.ts`
- Test: `apps/sdk-ts/tests/client.test.ts`

- [ ] **Step 1: 初始化 sdk-ts 包**

Create `apps/sdk-ts/package.json`:
```json
{
  "name": "@oat/sdk-ts",
  "version": "0.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@oat/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
```

Create `apps/sdk-ts/tsconfig.json`:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": { "outDir": "./dist", "rootDir": "./src" },
  "include": ["src/**/*", "tests/**/*"]
}
```

- [ ] **Step 2: 写失败测试——上下文父子关系**

Create `apps/sdk-ts/tests/context.test.ts`:
```typescript
import { describe, it, expect } from 'vitest';
import { withObservationContext, getCurrentObservationId, getCurrentParentId } from '../src/context.js';

describe('观察点上下文', () => {
  it('嵌套调用时父 id 正确传递', async () => {
    let outerId: string | undefined;
    let innerParentId: string | undefined;

    // withObservationContext(id, fn)：把自己的 id 设为当前，父 id 取自外层
    await withObservationContext('outer', async () => {
      outerId = getCurrentObservationId();
      await withObservationContext('inner', async () => {
        // 内层：自己的 id 是 inner，parent 应是外层的 outer
        innerParentId = getCurrentParentId();
      });
    });

    expect(outerId).toBe('outer');
    expect(innerParentId).toBe('outer');
  });

  it('顶层无 parent', () => {
    expect(getCurrentObservationId()).toBeUndefined();
    expect(getCurrentParentId()).toBeNull();
  });
});
```

- [ ] **Step 3: 运行测试确认失败**

Run: `pnpm --filter @oat/sdk-ts test`
Expected: FAIL，模块未定义。

- [ ] **Step 4: 实现 context（AsyncLocalStorage）**

Create `apps/sdk-ts/src/context.ts`:
```typescript
import { AsyncLocalStorage } from 'node:async_hooks';

// 每个上下文节点存自己的 id 和父 id
type ContextNode = { id: string; parentId: string | null };
const storage = new AsyncLocalStorage<ContextNode>();

export function getCurrentObservationId(): string | undefined {
  return storage.getStore()?.id;
}

export function getCurrentParentId(): string | null {
  return storage.getStore()?.parentId ?? null;
}

// 进入一个新的 observation 上下文：把自己的 id 设为当前，父 id 取自外层
export async function withObservationContext<T>(id: string, fn: () => Promise<T>): Promise<T> {
  const parentId = getCurrentObservationId() ?? null;
  return storage.run({ id, parentId }, fn);
}
```
> 为什么 AsyncLocalStorage：它是 Node 的"请求级上下文"机制，类似 thread-local 但对 async 友好。嵌套调用 traceable 时，内层能自动拿到外层的 id 作为 parentId——这就是 trace 自动成树的秘密。

- [ ] **Step 5: 运行测试确认通过**

Run: `pnpm --filter @oat/sdk-ts test`
Expected: context 测试 PASS。

- [ ] **Step 6: 写 client 测试——批量缓冲**

Create `apps/sdk-ts/tests/client.test.ts`:
```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import { OATClient } from '../src/client.js';

function mockFetch() {
  const calls: any[] = [];
  const fetchFn = vi.fn(async (url: string, init: any) => {
    calls.push({ url, body: JSON.parse(init.body) });
    return new Response('{}', { status: 202 });
  });
  return { fetchFn, calls };
}

describe('OATClient 批量上报', () => {
  beforeEach(() => vi.useFakeTimers());
  afterEach(() => vi.useRealTimers());

  it('攒到阈值后批量发送', async () => {
    const { fetchFn, calls } = mockFetch();
    const client = new OATClient({
      baseUrl: 'http://localhost:3001',
      apiKey: 'key',
      flushAt: 2, // 攒到 2 条就发
      flushInterval: 99999,
      fetch: fetchFn as any,
    });

    client.enqueue({ id: '1', traceId: 't1', parentId: null, type: 'span', name: 'a', startTime: new Date().toISOString() });
    expect(fetchFn).not.toHaveBeenCalled();

    client.enqueue({ id: '2', traceId: 't1', parentId: null, type: 'span', name: 'b', startTime: new Date().toISOString() });
    await vi.waitFor(() => expect(fetchFn).toHaveBeenCalledTimes(1));
    expect(calls[0].body.batch).toHaveLength(2);
  });
});
```
> 为什么可注入 fetch：测试时不发真实网络请求，注入 mock fetch 断言上报 payload。

- [ ] **Step 7: 运行测试确认失败**

Run: `pnpm --filter @oat/sdk-ts test`
Expected: FAIL，OATClient 未定义。

- [ ] **Step 8: 实现 client（批量缓冲 + 定时 flush）**

Create `apps/sdk-ts/src/client.ts`:
```typescript
import type { Observation } from '@oat/shared';

export interface OATClientConfig {
  baseUrl: string;
  apiKey: string;
  flushAt?: number;       // 攒多少条发送，默认 50
  flushInterval?: number; // 多少毫秒发送，默认 1000
  fetch?: typeof fetch;   // 可注入，测试用
}

export class OATClient {
  private queue: Observation[] = [];
  private apiKey: string;
  private flushAt: number;
  private flushInterval: number;
  private fetchFn: typeof fetch;
  private timer: ReturnType<typeof setInterval> | null = null;

  constructor(config: OATClientConfig) {
    this.apiKey = config.apiKey;
    this.flushAt = config.flushAt ?? 50;
    this.flushInterval = config.flushInterval ?? 1000;
    this.fetchFn = config.fetch ?? fetch;
    this.startTimer();
  }

  enqueue(obs: Observation) {
    this.queue.push(obs);
    if (this.queue.length >= this.flushAt) {
      void this.flush();
    }
  }

  private startTimer() {
    this.timer = setInterval(() => { void this.flush(); }, this.flushInterval);
  }

  async flush() {
    if (this.queue.length === 0) return;
    const batch = this.queue.splice(0, this.queue.length);
    try {
      await this.fetchFn(`${this.baseUrl}/api/public/ingestion`, {
        method: 'POST',
        headers: { 'content-type': 'application/json', authorization: `Bearer ${this.apiKey}` },
        body: JSON.stringify({ batch }),
      });
    } catch (err) {
      // 上报失败永不阻塞用户：丢弃批次并告警（避免无限堆积）
      console.warn('[OAT] 上报失败，丢弃批次：', err);
    }
  }

  // 测试或优雅关闭时调用
  async shutdown() {
    if (this.timer) clearInterval(this.timer);
    await this.flush();
  }
}
```
> 为什么上报失败只丢弃不重试：M1 保持简单。重试会导致队列无限增长拖垮用户进程（可观测工具绝不能搞挂用户业务）。后续可加"有限次重试 + 有界队列"。

- [ ] **Step 9: 运行测试确认通过**

Run: `pnpm --filter @oat/sdk-ts test`
Expected: client 测试 PASS。

- [ ] **Step 10: 实现 traceable 装饰器**

Create `apps/sdk-ts/src/traceable.ts`:
```typescript
import { randomUUID } from 'node:crypto';
import { withObservationContext, getCurrentParentId } from './context.js';
import type { OATClient } from './client.js';
import type { Observation } from '@oat/shared';

// 模块级默认 client（由 init() 设置）
let defaultClient: OATClient | null = null;
export function setDefaultClient(client: OATClient | null) { defaultClient = client; }

type TraceableOptions = { name?: string; type?: Observation['type'] };

// traceable：包裹任意 async 函数，自动记录输入/输出/耗时，靠上下文形成父子树
export function traceable<TArgs extends any[], TResult>(
  fn: (...args: TArgs) => Promise<TResult>,
  options: TraceableOptions = {},
): (...args: TArgs) => Promise<TResult> {
  return async (...args: TArgs) => {
    const id = randomUUID();
    const name = options.name ?? fn.name ?? 'anonymous';
    const startTime = new Date();

    return withObservationContext(id, async () => {
      try {
        const result = await fn(...args);
        const endTime = new Date();
        if (defaultClient) {
          defaultClient.enqueue({
            id, traceId: getOrInitTraceId(), parentId: getCurrentParentId(),
            type: options.type ?? 'span', name, startTime: startTime.toISOString(), endTime: endTime.toISOString(),
            input: args, output: result,
          });
        }
        return result;
      } catch (err) {
        const endTime = new Date();
        if (defaultClient) {
          defaultClient.enqueue({
            id, traceId: getOrInitTraceId(), parentId: getCurrentParentId(),
            type: options.type ?? 'span', name, startTime: startTime.toISOString(), endTime: endTime.toISOString(),
            level: 'error', output: { error: String(err) },
          });
        }
        throw err;
      }
    });
  };
}

// M1 简化：用模块级 traceId，每个进程一个 trace（后续改为按请求/per-run 生成）
let _traceId: string = randomUUID();
function getOrInitTraceId(): string { return _traceId; }
export function resetTraceId(id?: string) { _traceId = id ?? randomUUID(); }
```
> M1 简化说明：traceId 用模块级单例，适合演示。M2 会改为在顶层 `withTrace()` 生成并透传，支持一个进程多 trace。

- [ ] **Step 11: 导出 index.ts**

Create `apps/sdk-ts/src/index.ts`:
```typescript
export { OATClient } from './client.js';
export type { OATClientConfig } from './client.js';
export { traceable, setDefaultClient, resetTraceId } from './traceable.js';
export { withObservationContext, getCurrentObservationId, getCurrentParentId } from './context.js';
```

- [ ] **Step 12: 类型检查**

Run: `pnpm --filter @oat/sdk-ts lint`
Expected: 无错误。

- [ ] **Step 13: 提交**

```bash
git add -A
git commit -m "feat(sdk-ts): 添加上下文、批量客户端与 traceable 装饰器"
```

---

## Task 8: Next.js Web 前端——Trace 列表页

**Files:**
- Create: `apps/web/package.json`
- Create: `apps/web/next.config.js`
- Create: `apps/web/tsconfig.json`
- Create: `apps/web/tailwind.config.ts`
- Create: `apps/web/postcss.config.js`
- Create: `apps/web/src/app/layout.tsx`
- Create: `apps/web/src/app/page.tsx`
- Create: `apps/web/src/app/globals.css`
- Create: `apps/web/src/lib/api.ts`

- [ ] **Step 1: 初始化 web 包**

Create `apps/web/package.json`:
```json
{
  "name": "@oat/web",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3000",
    "build": "next build",
    "start": "next start -p 3000",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "^14.2.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0",
    "tailwindcss": "^3.4.0",
    "typescript": "^5.4.0"
  }
}
```

Create `apps/web/tsconfig.json`:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["dom", "dom.iterable", "ES2022"],
    "jsx": "preserve",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "noEmit": true,
    "plugins": [{ "name": "next" }],
    "paths": { "@/*": ["./src/*"] }
  },
  "include": ["next-env.d.ts", "src/**/*", ".next/types/**/*.ts"]
}
```

Create `apps/web/next.config.js`:
```javascript
/** @type {import('next').NextConfig} */
const nextConfig = {
  transpilePackages: [],
  // 浏览器请求后端 API 时的代理，避免跨域（开发用）
  async rewrites() {
    return [
      { source: '/api/:path*', destination: `${process.env.SERVER_URL ?? 'http://localhost:3001'}/api/:path*` },
    ];
  },
};
export default nextConfig;
```

- [ ] **Step 2: 安装并配置 Tailwind**

Run: `pnpm install`

Create `apps/web/postcss.config.js`:
```javascript
export default { plugins: { tailwindcss: {}, autoprefixer: {} } };
```

Create `apps/web/tailwind.config.ts`:
```typescript
import type { Config } from 'tailwindcss';
const config: Config = {
  content: ['./src/**/*.{ts,tsx}'],
  theme: { extend: {} },
  plugins: [],
};
export default config;
```

Create `apps/web/src/app/globals.css`:
```css
@tailwind base;
@tailwind components;
@tailwind utilities;
```

- [ ] **Step 3: 编写 API 客户端**

Create `apps/web/src/lib/api.ts`:
```typescript
export type TraceListItem = {
  id: string;
  name: string;
  userId: string | null;
  sessionId: string | null;
  timestamp: string;
};

// 注意：projectId 在 M1 用一个固定的种子项目，后续加项目选择器
export async function listTraces(projectId: string): Promise<TraceListItem[]> {
  const res = await fetch(`/api/traces?projectId=${projectId}`, { cache: 'no-store' });
  if (!res.ok) throw new Error(`查询 traces 失败: ${res.status}`);
  const data = await res.json();
  return data.traces;
}
```

- [ ] **Step 4: 编写 layout 与列表页**

Create `apps/web/src/app/layout.tsx`:
```typescript
import './globals.css';
import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'OpenAgentTelemetry' };

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="zh">
      <body className="min-h-screen bg-gray-50 text-gray-900">{children}</body>
    </html>
  );
}
```

Create `apps/web/src/app/page.tsx`:
```typescript
import { listTraces } from '@/lib/api';

// M1 用环境变量配置的种子项目 id（在 docker-compose 启动时注入）
const SEED_PROJECT_ID = process.env.SEED_PROJECT_ID ?? '';

export default async function HomePage() {
  let traces: Awaited<ReturnType<typeof listTraces>> = [];
  let error: string | null = null;
  try {
    if (SEED_PROJECT_ID) traces = await listTraces(SEED_PROJECT_ID);
  } catch (e) {
    error = (e as Error).message;
  }

  return (
    <main className="mx-auto max-w-5xl p-8">
      <h1 className="text-2xl font-bold mb-6">Traces</h1>
      {error && <p className="text-red-600 mb-4">加载失败：{error}</p>}
      <div className="rounded-lg border bg-white overflow-hidden">
        <table className="w-full text-sm">
          <thead className="bg-gray-100 text-gray-600">
            <tr>
              <th className="text-left px-4 py-2">名称</th>
              <th className="text-left px-4 py-2">用户</th>
              <th className="text-left px-4 py-2">会话</th>
              <th className="text-left px-4 py-2">时间</th>
            </tr>
          </thead>
          <tbody>
            {traces.length === 0 && !error && (
              <tr><td colSpan={4} className="px-4 py-8 text-center text-gray-400">暂无 trace，用 SDK 上报一条试试</td></tr>
            )}
            {traces.map((t) => (
              <tr key={t.id} className="border-t hover:bg-gray-50">
                <td className="px-4 py-2 font-medium">{t.name}</td>
                <td className="px-4 py-2 text-gray-500">{t.userId ?? '-'}</td>
                <td className="px-4 py-2 text-gray-500">{t.sessionId ?? '-'}</td>
                <td className="px-4 py-2 text-gray-500">{new Date(t.timestamp).toLocaleString()}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </main>
  );
}
```

- [ ] **Step 5: 类型检查**

Run: `pnpm --filter @oat/web lint`
Expected: 无错误。

- [ ] **Step 6: 提交**

```bash
git add -A
git commit -m "feat(web): 添加 Next.js trace 列表页"
```

---

## Task 9: docker-compose 与端到端验证

**Files:**
- Create: `docker-compose.yml`
- Create: `apps/server/Dockerfile`
- Create: `apps/web/Dockerfile`
- Create: `scripts/seed.ts`（创建种子 project）
- Create: `apps/web/.env.local`（不提交）

- [ ] **Step 1: 编写 server Dockerfile**

Create `apps/server/Dockerfile`:
```dockerfile
FROM node:20-slim
RUN corepack enable
WORKDIR /app
# 先拷工作区配置
COPY package.json pnpm-workspace.yaml pnpm-lock.yaml* ./
COPY tsconfig.base.json ./
COPY packages ./packages
COPY apps/server ./apps/server
COPY apps/sdk-ts ./apps/sdk-ts
RUN pnpm install --frozen-lockfile
WORKDIR /app/apps/server
EXPOSE 3001
CMD ["pnpm", "dev"]
```

- [ ] **Step 2: 编写 web Dockerfile**

Create `apps/web/Dockerfile`:
```dockerfile
FROM node:20-slim
RUN corepack enable
WORKDIR /app
COPY package.json pnpm-workspace.yaml pnpm-lock.yaml* ./
COPY tsconfig.base.json ./
COPY packages ./packages
COPY apps/web ./apps/web
RUN pnpm install --frozen-lockfile
WORKDIR /app/apps/web
EXPOSE 3000
CMD ["pnpm", "dev"]
```

- [ ] **Step 3: 编写 docker-compose**

Create `docker-compose.yml`:
```yaml
version: "3.9"
services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: oat
      POSTGRES_PASSWORD: oat
      POSTGRES_DB: oat
    ports:
      - "5432:5432"
    volumes:
      - oat-pg:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U oat"]
      interval: 5s
      timeout: 3s
      retries: 10

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  server:
    build:
      context: .
      dockerfile: apps/server/Dockerfile
    environment:
      DATABASE_URL: postgresql://oat:oat@postgres:5432/oat
      PORT: "3001"
    ports:
      - "3001:3001"
    depends_on:
      postgres:
        condition: service_healthy

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    environment:
      SERVER_URL: http://server:3001
      SEED_PROJECT_ID: "${SEED_PROJECT_ID}"
    ports:
      - "3000:3000"
    depends_on:
      - server

volumes:
  oat-pg:
```

- [ ] **Step 4: 编写种子脚本（创建测试 project + 跑迁移）**

Create `scripts/seed.ts`:
```typescript
// 用法：DATABASE_URL=... tsx scripts/seed.ts
// 作用：跑迁移并插入一个种子 project，输出它的 id 和 apiKey
import { drizzle } from 'drizzle-orm/postgres-js';
import postgres from 'postgres';
import { migrate } from 'drizzle-orm/postgres-js/migrator';
import { projects } from '../apps/server/src/db/schema.js';

const url = process.env.DATABASE_URL!;
const sql = postgres(url, { max: 1 });
const db = drizzle(sql);

async function main() {
  await migrate(db, { migrationsFolder: './apps/server/drizzle' });
  const [proj] = await db
    .insert(projects)
    .values({ name: 'Demo Project', slug: 'demo', apiKey: 'demo-api-key' })
    .onConflictDoUpdate({ target: projects.slug, set: { name: 'Demo Project' } })
    .returning();
  console.log(JSON.stringify({ projectId: proj.id, apiKey: proj.apiKey }));
  await sql.end();
}
main();
```

- [ ] **Step 5: 启动全栈并初始化**

```bash
docker-compose up -d postgres redis
# 等待 postgres 就绪后跑迁移+种子
DATABASE_URL=postgresql://oat:oat@localhost:5432/oat pnpm --filter @oat/server exec tsx ../../scripts/seed.ts
# 输出形如 {"projectId":"...","apiKey":"demo-api-key"}
# 启动 server 与 web
docker-compose up -d server web
```

- [ ] **Step 6: 端到端验证——curl 上报**

```bash
# 用种子 apiKey 上报一条 trace
curl -X POST http://localhost:3001/api/public/ingestion \
  -H "authorization: Bearer demo-api-key" \
  -H "content-type: application/json" \
  -d '{"batch":[{"id":"o1","traceId":"t1","parentId":null,"type":"span","name":"端到端测试","startTime":"2026-07-09T00:00:00Z","endTime":"2026-07-09T00:00:01Z","input":{"q":"hi"}}}]}'
# 期望：{"accepted":1}
```

- [ ] **Step 7: 端到端验证——Web 看列表**

浏览器打开 `http://localhost:3000`（设置 `SEED_PROJECT_ID` 环境变量为种子输出的 projectId），确认表格里出现"端到端测试"。

- [ ] **Step 8: 端到端验证——SDK 上报**

创建临时验证脚本 `scripts/verify-sdk.ts`:
```typescript
import { OATClient, traceable, setDefaultClient, resetTraceId } from '../apps/sdk-ts/src/index.js';

const client = new OATClient({ baseUrl: 'http://localhost:3001', apiKey: 'demo-api-key', flushAt: 1, flushInterval: 1000 });
setDefaultClient(client);
resetTraceId(crypto.randomUUID());

const greet = traceable(async (name: string) => `Hello, ${name}!`, { name: 'greet' });
await greet('World');
await client.shutdown();
console.log('SDK 上报完成');
```

Run: `DATABASE_URL=... tsx scripts/verify-sdk.ts`（实际不需要 DATABASE_URL，但 tsx 启动需要；可去掉）
Expected: 控制台输出"SDK 上报完成"，刷新 Web 看到新 trace。

- [ ] **Step 9: 提交**

```bash
git add -A
git commit -m "feat: 添加 docker-compose 与端到端验证（M1 骨架打通完成）"
git push origin main
```

---

## 验收标准（M1 Definition of Done）

- [ ] `docker-compose up` 能起 Postgres + Redis + server + web 四个服务
- [ ] 跑种子脚本后，有一个可用的 project（apiKey=`demo-api-key`）
- [ ] `POST /api/public/ingestion` 带 apiKey 上报后返回 202，数据库有记录
- [ ] `GET /api/traces?projectId=...` 返回 trace 列表
- [ ] Web `http://localhost:3000` 显示 trace 列表
- [ ] TS SDK 的 `traceable` + `OATClient` 上报后，Web 能看到
- [ ] 所有单元/API 测试 `pnpm test` 通过
- [ ] 类型检查 `pnpm lint` 无错误

---

## 自检备注

- **traceable 的 traceId**：M1 用模块级单例简化，足够演示端到端；M2 改为 `withTrace()` 按 run 生成。
- **OTLP 兼容后门**：M1 暂未实现 `/v1/traces` OTLP 端点（设计里有），留到 M2 与异步队列一起做，避免 M1 膨胀。
- **异步队列**：M1 ingestion 同步写库（简单可验证）；M2 引入 BullMQ 改为 202+入队。
