# M13 评估任务（Eval Jobs）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 OAT 平台内置 LLM 跑 Prompt 对 Dataset 每个样例执行评估，用 LLM-as-judge + 数值阈值打分，产出可钻取调试的 trace。

**Architecture:** 进程内 Worker（并发控制 + 取消 + 崩溃恢复）执行每个 case，调 OpenAI 兼容 LLM API → 创建 trace + scores（source='eval_job'）→ EventBus/SSE 推进度。全局 Provider 注册表存 AES-256-GCM 加密的 API key。

**Tech Stack:** Fastify + Drizzle ORM + PostgreSQL + Next.js App Router + AES-256-GCM (Node.js crypto)

**Spec**: [docs/specs/2026-07-13-m13-eval-jobs-design.md](../specs/2026-07-13-m13-eval-jobs-design.md)

---

## Task 1: crypto.ts + ENCRYPTION_KEY 启动校验

**Files:**
- Create: `apps/server/src/modules/crypto.ts`
- Test: `apps/server/tests/crypto.test.ts`
- Modify: `apps/server/src/server.ts`

- [ ] **Step 1: 写失败测试** — `apps/server/tests/crypto.test.ts`

```typescript
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import { aesGcmEncrypt, aesGcmDecrypt, validateEncryptionKey } from '../src/modules/crypto.js';

describe('crypto', () => {
  const originalKey = process.env.ENCRYPTION_KEY;
  beforeEach(() => { process.env.ENCRYPTION_KEY = 'dGVzdC1rZXktMzItYnl0ZXMtbG9uZy1lbm91Z2hISE='; });
  afterEach(() => { if (originalKey) process.env.ENCRYPTION_KEY = originalKey; else delete process.env.ENCRYPTION_KEY; });

  it('加密→解密还原原文', () => {
    const enc = aesGcmEncrypt('sk-proj-abc123xyz');
    expect(enc).not.toBe('sk-proj-abc123xyz');
    expect(aesGcmDecrypt(enc)).toBe('sk-proj-abc123xyz');
  });
  it('每次加密密文不同（IV 随机）', () => {
    const a = aesGcmEncrypt('hello'); const b = aesGcmEncrypt('hello');
    expect(a).not.toBe(b);
    expect(aesGcmDecrypt(a)).toBe('hello');
  });
  it('密文含 IV+authTag+ciphertext', () => {
    const decoded = Buffer.from(aesGcmEncrypt('test'), 'base64');
    expect(decoded.length).toBeGreaterThan(28); // IV(12)+authTag(16)+ct
  });
  it('错误 key 解密失败', () => {
    const enc = aesGcmEncrypt('secret');
    process.env.ENCRYPTION_KEY = Buffer.alloc(32, 1).toString('base64');
    expect(() => aesGcmDecrypt(enc)).toThrow();
  });
  it('空 key 校验抛错', () => {
    delete process.env.ENCRYPTION_KEY;
    expect(() => validateEncryptionKey()).toThrow('ENCRYPTION_KEY');
  });
  it('validateEncryptionKey 正常时不抛错', () => {
    expect(() => validateEncryptionKey()).not.toThrow();
  });
});
```

- [ ] **Step 2: 运行验证失败** — `cd apps/server && npx vitest run tests/crypto.test.ts` → FAIL（模块不存在）

- [ ] **Step 3: 实现 crypto.ts** — `apps/server/src/modules/crypto.ts`

```typescript
import { createCipheriv, createDecipheriv, randomBytes } from 'node:crypto';

const ALGO = 'aes-256-gcm';
const IV_LEN = 12;

function getKey(): Buffer {
  const raw = process.env.ENCRYPTION_KEY;
  if (!raw) throw new Error('ENCRYPTION_KEY 未设置');
  return Buffer.from(raw, 'base64');
}

export function validateEncryptionKey(): void {
  const raw = process.env.ENCRYPTION_KEY;
  if (!raw) throw new Error('ENCRYPTION_KEY 未设置，请运行 openssl rand -base64 32');
  const buf = Buffer.from(raw, 'base64');
  if (buf.length !== 32) throw new Error(`ENCRYPTION_KEY 必须 32 字节，当前 ${buf.length}`);
}

export function aesGcmEncrypt(plaintext: string): string {
  const iv = randomBytes(IV_LEN);
  const cipher = createCipheriv(ALGO, getKey(), iv);
  const encrypted = Buffer.concat([cipher.update(plaintext, 'utf8'), cipher.final()]);
  const authTag = cipher.getAuthTag();
  return Buffer.concat([iv, authTag, encrypted]).toString('base64');
}

export function aesGcmDecrypt(ciphertextB64: string): string {
  const buf = Buffer.from(ciphertextB64, 'base64');
  const iv = buf.subarray(0, IV_LEN);
  const authTag = buf.subarray(IV_LEN, IV_LEN + 16);
  const encrypted = buf.subarray(IV_LEN + 16);
  const decipher = createDecipheriv(ALGO, getKey(), iv);
  decipher.setAuthTag(authTag);
  return Buffer.concat([decipher.update(encrypted), decipher.final()]).toString('utf8');
}
```

- [ ] **Step 4: 运行验证通过** — `cd apps/server && npx vitest run tests/crypto.test.ts` → PASS

- [ ] **Step 5: server.ts 启动校验** — 在 `buildServer` 函数开头加 `validateEncryptionKey()` 调用 + import

- [ ] **Step 6: 提交** — `git add -A && git commit -m "feat(m13): crypto.ts AES-256-GCM 加解密 + ENCRYPTION_KEY 启动校验"`

---

## Task 2: schema 4 张表 + 迁移

**Files:**
- Modify: `apps/server/src/db/schema.ts`（末尾追加）
- Create: `apps/server/drizzle/0007_eval_tables.sql`
- Modify: `apps/server/drizzle/meta/_journal.json`

- [ ] **Step 1: schema.ts 末尾追加 4 张表**

```typescript
export const llmProviders = pgTable('llm_providers', {
  id: uuid('id').primaryKey().defaultRandom(),
  name: text('name').notNull(),
  provider: text('provider').notNull(),
  baseURL: text('base_url').notNull(),
  apiKeyEnc: text('api_key_enc').notNull(),
  apiKeyPreview: text('api_key_preview').notNull(),
  defaultModel: text('default_model'),
  createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).notNull().defaultNow(),
});

export const evaluators = pgTable('evaluators', {
  id: uuid('id').primaryKey().defaultRandom(),
  projectId: uuid('project_id').notNull().references(() => projects.id),
  name: text('name').notNull(),
  type: text('type').notNull(),
  config: jsonb('config').notNull().default({}),
  createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).notNull().defaultNow(),
});

export const evalJobs = pgTable('eval_jobs', {
  id: uuid('id').primaryKey().defaultRandom(),
  projectId: uuid('project_id').notNull().references(() => projects.id),
  name: text('name').notNull(),
  datasetId: uuid('dataset_id').notNull().references(() => datasets.id),
  promptId: uuid('prompt_id').notNull().references(() => prompts.id),
  promptVersion: integer('prompt_version').notNull(),
  providerId: uuid('provider_id').notNull().references(() => llmProviders.id),
  model: text('model').notNull(),
  evaluatorIds: uuid('evaluator_ids').array().notNull().default([]),
  status: text('status').notNull().default('pending'),
  concurrency: integer('concurrency').notNull().default(3),
  totalItems: integer('total_items').notNull().default(0),
  completedItems: integer('completed_items').notNull().default(0),
  failedItems: integer('failed_items').notNull().default(0),
  summary: jsonb('summary').default({}),
  errorMessage: text('error_message'),
  startedAt: timestamp('started_at', { withTimezone: true }),
  completedAt: timestamp('completed_at', { withTimezone: true }),
  createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow(),
});

export const evalJobItems = pgTable('eval_job_items', {
  id: uuid('id').primaryKey().defaultRandom(),
  jobId: uuid('job_id').notNull().references(() => evalJobs.id).onDelete('cascade'),
  datasetItemId: uuid('dataset_item_id').notNull().references(() => datasetItems.id),
  status: text('status').notNull().default('pending'),
  output: jsonb('output').default(null),
  traceId: uuid('trace_id'),
  latencyMs: integer('latency_ms'),
  errorMessage: text('error_message'),
  startedAt: timestamp('started_at', { withTimezone: true }),
  completedAt: timestamp('completed_at', { withTimezone: true }),
});
```

- [ ] **Step 2: 创建迁移 SQL** — `apps/server/drizzle/0007_eval_tables.sql`（CREATE TABLE 4 张 + 5 个索引，参考 spec）

- [ ] **Step 3: 更新 _journal.json** — 加 idx 7 条目

- [ ] **Step 4: 应用迁移** — `psql "postgresql://oat:oat@localhost:5432/oat" -f apps/server/drizzle/0007_eval_tables.sql`

- [ ] **Step 5: 提交** — `git add -A && git commit -m "feat(m13): schema 新增 4 张表 + 迁移"`

---

## Task 3: ProviderRepository + CRUD 路由 + 测试

**Files:**
- Create: `apps/server/src/repositories/provider-repository.ts`
- Create: `apps/server/src/routes/eval-providers.ts`
- Test: `apps/server/tests/eval-providers.test.ts`
- Modify: `apps/server/src/repositories/index.ts`, `apps/server/src/app.ts`

- [ ] **Step 1: 实现 ProviderRepository** — IProviderRepository 接口 + PostgresProviderRepository（create/list/get/getWithKey/update/delete）。create 时 aesGcmEncrypt(apiKey) + maskKey 末4位。GET 永不返回 apiKeyEnc。

- [ ] **Step 2: 实现 CRUD 路由** — `buildEvalProviderRoutes(deps)` 工厂。GET/POST/PUT/DELETE + POST /:id/test。zod 校验 provider 枚举 openai/custom/ollama。

- [ ] **Step 3: app.ts 集成** — AppDeps 加 `providerRepo: IProviderRepository`，注册路由

- [ ] **Step 4: 写测试** — 创建→GET 不返回 key→更新→删除。mockKey `'****5678'`

- [ ] **Step 5: 运行 + 提交** — `git commit -m "feat(m13): ProviderRepository + CRUD 路由 + 测试"`

---

## Task 4: llm-client.ts + 测试

**Files:**
- Create: `apps/server/src/modules/llm-client.ts`
- Test: `apps/server/tests/llm-client.test.ts`

- [ ] **Step 1: 写测试** — 5 个用例：解析 OpenAI 格式、HTTP 错误抛出、空 choices 抛错、usage 缺失 tokens=null、Authorization header 正确

- [ ] **Step 2: 实现 llm-client.ts** — `chatComplete(provider, model, messages)` 调 `${baseURL}/chat/completions`，30s AbortController 超时，返回 `{ content, promptTokens, completionTokens }`

- [ ] **Step 3: 运行 + 提交** — `git commit -m "feat(m13): llm-client OpenAI 兼容调用 + 5 个测试"`

---

## Task 5: EvaluatorRepository + CRUD 路由 + 测试

**Files:**
- Create: `apps/server/src/repositories/evaluator-repository.ts`
- Create: `apps/server/src/routes/eval-evaluators.ts`
- Test: `apps/server/tests/eval-evaluators.test.ts`
- Modify: `apps/server/src/repositories/index.ts`, `apps/server/src/app.ts`

- [ ] **Step 1: 实现 EvaluatorRepository** — create/list/get/update/delete，项目级

- [ ] **Step 2: 实现 CRUD 路由** — zod 校验 type + config 匹配（llm_judge 需 providerId/model/judgePrompt/min/max；numeric_threshold 需 metric/operator/threshold/passScore/failScore）

- [ ] **Step 3: app.ts 集成** — AppDeps 加 evaluatorRepo，注册路由

- [ ] **Step 4: 写测试** — 创建 llm_judge、创建 numeric_threshold、config 不匹配报错、列表按 projectId 过滤

- [ ] **Step 5: 运行 + 提交** — `git commit -m "feat(m13): EvaluatorRepository + CRUD 路由 + 测试"`

---

## Task 6: derive-action 扩展 + event-bus 类型

**Files:**
- Modify: `apps/server/src/modules/derive-action.ts`
- Modify: `apps/server/src/modules/event-bus.ts`
- Modify: `apps/server/tests/derive-action.test.ts`

- [ ] **Step 1: derive-action 加映射** — `/api/eval/providers`→eval_provider, `/api/eval/evaluators`→evaluator, `/api/eval/jobs`→eval_job, `/api/eval/jobs/:id/cancel`→eval_job.cancel（cancel 在 resource 匹配前特殊处理）

- [ ] **Step 2: event-bus 加类型** — `EvalJobStartedEvent { jobId }`, `EvalItemCompletedEvent { jobId, item, scores }`

- [ ] **Step 3: 加 4 个测试** — eval_provider.create / evaluator.update / eval_job.create / eval_job.cancel

- [ ] **Step 4: 运行 + 提交** — `git commit -m "feat(m13): derive-action 扩展 eval 资源 + event-bus eval 事件类型"`

---

## Task 7: EvalJobRepository（jobs + items + 状态机）

**Files:**
- Create: `apps/server/src/repositories/eval-job-repository.ts`
- Modify: `apps/server/src/repositories/index.ts`

- [ ] **Step 1: 实现 EvalJobRepository** — IEvalJobRepository 接口 + PostgresEvalJobRepository。方法：createJob, createItems, listJobs(cursor 分页), getJob, updateJobStatus, incrementJobCounter(sql 自增), setJobSummary, listPendingItems, getItem, updateItemStatus, listItems(cursor 分页), countItemsByStatus, interruptRunning(崩溃恢复)

- [ ] **Step 2: index.ts 导出**

- [ ] **Step 3: 提交** — `git commit -m "feat(m13): EvalJobRepository — 状态机 + 崩溃恢复"`

---

## Task 8: eval-worker.ts（状态机 + 并发 + 取消 + 恢复）

**Files:**
- Create: `apps/server/src/modules/eval-worker.ts`
- Test: `apps/server/tests/eval-worker.test.ts`
- Modify: `apps/server/src/app.ts`, `apps/server/src/server.ts`

- [ ] **Step 1: 实现 EvalWorker 类** — 单例，MAX_CONCURRENCY=3。constructor 监听 `eval:job-started` → loadPendingItems → pump → runItem。runItem 流程：标记 running → 加载 datasetItem+prompt → 渲染 → chatComplete → 创建 trace+observation → 跑 evaluators（numeric_threshold 从 trace 取值 / llm_judge 调 judge LLM）→ 写 scores(source='eval_job') → 标记 success → emit `eval:item-completed` → 检查 job 完结。cancelJob 加 cancelledJobs set。测试 mock 全部依赖。

- [ ] **Step 2: app.ts 加 evalJobRepo + evalWorker 到 AppDeps**

- [ ] **Step 3: server.ts 启动调 `evalJobRepo.interruptRunning()`**

- [ ] **Step 4: 运行 + 提交** — `git commit -m "feat(m13): eval-worker 进程内 worker"`

---

## Task 9: Job API（创建/触发/取消/列表/详情/items）+ 测试

**Files:**
- Create: `apps/server/src/routes/eval-jobs.ts`
- Test: `apps/server/tests/eval-jobs.test.ts`
- Modify: `apps/server/src/app.ts`

- [ ] **Step 1: 实现 Job 路由** — `buildEvalJobRoutes(deps)`。POST /api/eval/jobs（创建 job + items + emit job-started）、GET /api/eval/jobs（cursor 分页）、GET /api/eval/jobs/:id（详情）、GET /api/eval/jobs/:id/items（cursor 分页）、POST /api/eval/jobs/:id/cancel（调 evalWorker.cancelJob）、DELETE /api/eval/jobs/:id

- [ ] **Step 2: 写测试** — 创建 job 验证 items 生成、列表分页、取消

- [ ] **Step 3: 运行 + 提交** — `git commit -m "feat(m13): Job API + 测试"`

---

## Task 10: SSE /api/stream/eval/:jobId + 测试

**Files:**
- Modify: `apps/server/src/routes/stream.ts`
- Test: `apps/server/tests/eval-sse.test.ts`

- [ ] **Step 1: stream.ts 加 eval SSE 端点** — `GET /api/stream/eval/:jobId`，config rateLimit:false。订阅 `eval:item-completed` 事件（过滤 jobId 匹配），30s 心跳，req.raw.on('close') 清理。参考现有 traces/alerts/audit SSE 模式。

- [ ] **Step 2: 写测试** — mock worker emit item-completed → SSE 收到事件

- [ ] **Step 3: 运行 + 提交** — `git commit -m "feat(m13): SSE eval 进度流 + 测试"`

---

## Task 11: 前端 Provider + Evaluator 管理页

**Files:**
- Create: `apps/web/src/app/eval/providers/page.tsx` + `ProviderListClient.tsx`
- Create: `apps/web/src/app/eval/evaluators/page.tsx` + `EvaluatorListClient.tsx`
- Modify: `apps/web/src/lib/api.shared.ts`, `apps/web/src/lib/api.client.ts`
- Modify: `apps/web/src/components/Nav.tsx`

- [ ] **Step 1: api.shared.ts 加类型** — LlmProvider, Evaluator, EvalJob, EvalJobItem 类型

- [ ] **Step 2: api.client.ts 加函数** — listProviders/createProvider/updateProvider/deleteProvider/testProvider + listEvaluators/createEvaluator/updateEvaluator/deleteEvaluator + listEvalJobs/createEvalJob/getEvalJob/listEvalJobItems/cancelEvalJob/deleteEvalJob

- [ ] **Step 3: Nav.tsx 加「评估」链接** — `{ href: '/eval', label: '评估' }`

- [ ] **Step 4: Provider 管理页** — RSC page.tsx + ProviderListClient（'use client'，列表 + 新建/编辑表单 + 测试连通按钮，apiKey 编辑时留空不改）

- [ ] **Step 5: Evaluator 管理页** — RSC page.tsx + EvaluatorListClient（列表 + 新建/编辑，两种 type 表单切换）

- [ ] **Step 6: 运行 lint + 提交** — `git commit -m "feat(m13): Provider + Evaluator 管理页 + Nav 链接"`

---

## Task 12: 前端 Job 列表 + 新建 + 运行详情页（SSE）

**Files:**
- Create: `apps/web/src/app/eval/page.tsx` + `EvalListClient.tsx`
- Create: `apps/web/src/app/eval/jobs/new/page.tsx` + `NewJobClient.tsx`
- Create: `apps/web/src/app/eval/jobs/[jobId]/page.tsx` + `JobDetailClient.tsx`
- Modify: `apps/web/src/app/page.tsx`（Traces 列表 eval 徽章）

- [ ] **Step 1: Job 列表页** — RSC + EvalListClient（按 projectId 列出 jobs，status 徽章，进度条，新建按钮）

- [ ] **Step 2: 新建任务表单** — RSC + NewJobClient（选 dataset→prompt+version→provider+model→勾选 evaluators→concurrency，提交跳转 /eval/[jobId]）

- [ ] **Step 3: 运行详情页** — RSC + JobDetailClient（summary 卡片 + 进度条 + SSE EventSource 订阅 eval:item-completed + item 表格 + 行展开 + 「查看 trace」链接）

- [ ] **Step 4: Traces 列表 eval 徽章** — page.tsx 检查 metadata.source==='eval' 加徽章

- [ ] **Step 5: lint + 提交** — `git commit -m "feat(m13): Job 列表/新建/运行详情页 SSE + Traces eval 徽章"`

---

## Task 13: verify-m13.sh + README 更新 + push

**Files:**
- Create: `scripts/verify-m13.sh`
- Modify: `README.md`, `README.zh-CN.md`, `docs/README.md`

- [ ] **Step 1: verify-m13.sh** — 起 mock LLM server（node -e 返回固定 chat completion）→ 创建 provider → 创建 evaluator → 创建 job → 验证 items 生成 → 验证 SSE 推送 → 验证 trace 落库 → 验证 scores 落库

- [ ] **Step 2: README 更新** — 加 M13 特性章节 + 路线图加 M13

- [ ] **Step 3: 全量 lint + test + 运行 verify + push**
